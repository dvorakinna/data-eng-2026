# Run `dbt deps` to install — packages land in analytics/dbt_packages/ (gitignored)
packages:
  - package: dbt-labs/dbt_utils   # macros: tests, surrogate keys, date_spine, pivot, etc.
    version: [">=1.1.0", "<2.0.0"]